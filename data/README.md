This is the directory where Processing will search for your data files.

For this sketch to run properly, you need to put your own `image.jpg` and `movie.mov` and `motion.mov` files inside of this directory.